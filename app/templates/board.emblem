h1 = title

= input#magicBar type="text" placeholder="type, friend" action="create" value=magicBar disabled=loggedOut

each widgets
  w-widget action="delete" param=this

h2 ADASD ASD SD

w-avatar

h2 ADASD ASD SD